// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCrtJsgPUX43HgGjTvLS9wAfdxiJ9o9oAU",
    authDomain: "testing-ngrx-fire.firebaseapp.com",
    databaseURL: "https://testing-ngrx-fire.firebaseio.com",
    projectId: "testing-ngrx-fire",
    storageBucket: "testing-ngrx-fire.appspot.com",
    messagingSenderId: "777339251483",
    appId: "1:777339251483:web:38a11ceee650190dc501be"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
